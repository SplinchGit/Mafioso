AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: Mafioso AWS-only web game stack

Parameters:
  Stage:
    Type: String
    Default: staging
    AllowedValues: [dev, staging, prod]

Globals:
  Function:
    Runtime: nodejs20.x
    Timeout: 30
    MemorySize: 256
    Layers:
      - !Ref RuntimeDependenciesLayer
    Environment:
      Variables:
        PLAYERS_TABLE: !Ref PlayersTable
        COOLDOWNS_TABLE: !Ref CooldownsTable
        CRIME_HISTORY_TABLE: !Ref CrimeHistoryTable
        CITY_PROPS_TABLE: !Ref CityPropsTable
        CAR_LISTINGS_TABLE: !Ref CarListingsTable
        CREWS_TABLE: !Ref CrewsTable
        BULLET_FACTORIES_TABLE: !Ref BulletFactoriesTable
        JWT_SECRET_ARN: !Ref JWTSecretArn
        JWT_SECRET: !Sub '{{resolve:secretsmanager:${JWTSecretArn}:SecretString}}'

Resources:
  JWTSecretArn:
    Type: AWS::SecretsManager::Secret
    DeletionPolicy: RetainExceptOnCreate
    UpdateReplacePolicy: Retain
    Properties:
      Name: !Sub 'mafioso-${Stage}-jwt'
      Description: Signing secret for Mafioso game sessions
      GenerateSecretString:
        PasswordLength: 64
        ExcludePunctuation: true

  RuntimeDependenciesLayer:
    Type: AWS::Serverless::LayerVersion
    Properties:
      LayerName: !Sub 'mafioso-${Stage}-runtime'
      Description: Runtime packages shared by the Mafioso Lambda functions
      ContentUri: ../backend/layer/
      CompatibleRuntimes: [nodejs20.x]

  MafiosoApi:
    Type: AWS::Serverless::Api
    Properties:
      StageName: !Ref Stage
      Cors:
        AllowMethods: "'GET,POST,OPTIONS'"
        AllowHeaders: "'Content-Type,Authorization'"
        AllowOrigin: "'*'"
      Auth:
        Authorizers:
          CognitoAuthorizer:
            UserPoolArn: !GetAtt UserPool.Arn

  UserPool:
    Type: AWS::Cognito::UserPool
    DeletionPolicy: RetainExceptOnCreate
    UpdateReplacePolicy: Retain
    Properties:
      UserPoolName: !Sub 'mafioso-${Stage}'
      UsernameAttributes: [email]
      AutoVerifiedAttributes: [email]
      AccountRecoverySetting:
        RecoveryMechanisms:
          - Name: verified_email
            Priority: 1
      Policies:
        PasswordPolicy:
          MinimumLength: 8
          RequireLowercase: true
          RequireUppercase: true
          RequireNumbers: true
          RequireSymbols: false
      Schema:
        - Name: email
          Required: true
          Mutable: true
        - Name: preferred_username
          Required: false
          Mutable: true

  UserPoolClient:
    Type: AWS::Cognito::UserPoolClient
    Properties:
      ClientName: !Sub 'mafioso-${Stage}-web'
      UserPoolId: !Ref UserPool
      GenerateSecret: false
      PreventUserExistenceErrors: ENABLED
      ExplicitAuthFlows:
        - ALLOW_USER_SRP_AUTH
        - ALLOW_REFRESH_TOKEN_AUTH

  PlayersTable:
    Type: AWS::DynamoDB::Table
    DeletionPolicy: RetainExceptOnCreate
    UpdateReplacePolicy: Retain
    Properties:
      TableName: !Sub 'mafioso-${Stage}-players-v2'
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: worldId
          AttributeType: S
        - AttributeName: username
          AttributeType: S
      KeySchema:
        - AttributeName: worldId
          KeyType: HASH
      GlobalSecondaryIndexes:
        - IndexName: username-index
          KeySchema:
            - AttributeName: username
              KeyType: HASH
          Projection:
            ProjectionType: ALL

  CooldownsTable:
    Type: AWS::DynamoDB::Table
    DeletionPolicy: RetainExceptOnCreate
    UpdateReplacePolicy: Retain
    Properties:
      TableName: !Sub 'mafioso-${Stage}-cooldowns-v2'
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: playerId
          AttributeType: S
        - AttributeName: crimeId
          AttributeType: N
      KeySchema:
        - AttributeName: playerId
          KeyType: HASH
        - AttributeName: crimeId
          KeyType: RANGE
      TimeToLiveSpecification:
        AttributeName: expiresAt
        Enabled: true

  CrimeHistoryTable:
    Type: AWS::DynamoDB::Table
    DeletionPolicy: RetainExceptOnCreate
    UpdateReplacePolicy: Retain
    Properties:
      TableName: !Sub 'mafioso-${Stage}-crime-history-v2'
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: id
          AttributeType: S
      KeySchema:
        - AttributeName: id
          KeyType: HASH

  CityPropsTable:
    Type: AWS::DynamoDB::Table
    DeletionPolicy: RetainExceptOnCreate
    UpdateReplacePolicy: Retain
    Properties:
      TableName: !Sub 'mafioso-${Stage}-city-props-v2'
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: propId
          AttributeType: S
      KeySchema:
        - AttributeName: propId
          KeyType: HASH

  CarListingsTable:
    Type: AWS::DynamoDB::Table
    DeletionPolicy: RetainExceptOnCreate
    UpdateReplacePolicy: Retain
    Properties:
      TableName: !Sub 'mafioso-${Stage}-car-listings-v2'
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: id
          AttributeType: S
      KeySchema:
        - AttributeName: id
          KeyType: HASH

  CrewsTable:
    Type: AWS::DynamoDB::Table
    DeletionPolicy: RetainExceptOnCreate
    UpdateReplacePolicy: Retain
    Properties:
      TableName: !Sub 'mafioso-${Stage}-crews-v2'
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: crewId
          AttributeType: S
      KeySchema:
        - AttributeName: crewId
          KeyType: HASH

  BulletFactoriesTable:
    Type: AWS::DynamoDB::Table
    DeletionPolicy: RetainExceptOnCreate
    UpdateReplacePolicy: Retain
    Properties:
      TableName: !Sub 'mafioso-${Stage}-bullet-factories-v2'
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: cityId
          AttributeType: N
      KeySchema:
        - AttributeName: cityId
          KeyType: HASH

  CognitoSessionFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ../backend/dist/
      Handler: backend/functions/auth/cognitoSession.handler
      Policies:
        - DynamoDBCrudPolicy: { TableName: !Ref PlayersTable }
        - Statement:
            - Effect: Allow
              Action: secretsmanager:GetSecretValue
              Resource: !Ref JWTSecretArn
      Events:
        Session:
          Type: Api
          Properties:
            RestApiId: !Ref MafiosoApi
            Path: /api/auth/session
            Method: post
            Auth:
              Authorizer: CognitoAuthorizer

  ValidateTokenFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ../backend/dist/
      Handler: backend/functions/auth/validateToken.handler
      Policies:
        - DynamoDBCrudPolicy: { TableName: !Ref PlayersTable }
        - Statement:
            - Effect: Allow
              Action: secretsmanager:GetSecretValue
              Resource: !Ref JWTSecretArn
      Events:
        Validate:
          Type: Api
          Properties: { RestApiId: !Ref MafiosoApi, Path: /api/auth/validate, Method: get }

  ProfileFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ../backend/dist/
      Handler: backend/functions/player/getProfile.handler
      Policies:
        - DynamoDBReadPolicy: { TableName: !Ref PlayersTable }
      Events:
        Profile:
          Type: Api
          Properties: { RestApiId: !Ref MafiosoApi, Path: /api/player/profile, Method: get }

  CommitCrimeFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ../backend/dist/
      Handler: backend/functions/crimes/commitCrime.handler
      Policies:
        - DynamoDBCrudPolicy: { TableName: !Ref PlayersTable }
        - DynamoDBCrudPolicy: { TableName: !Ref CooldownsTable }
        - DynamoDBCrudPolicy: { TableName: !Ref CrimeHistoryTable }
        - Statement:
            - Effect: Allow
              Action: secretsmanager:GetSecretValue
              Resource: !Ref JWTSecretArn
      Events:
        CommitCrime:
          Type: Api
          Properties: { RestApiId: !Ref MafiosoApi, Path: /api/crimes/commit, Method: post }

  BuyGunFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ../backend/dist/
      Handler: backend/functions/player/buyGun.handler
      Policies:
        - DynamoDBCrudPolicy: { TableName: !Ref PlayersTable }
      Events:
        BuyGun:
          Type: Api
          Properties: { RestApiId: !Ref MafiosoApi, Path: /api/player/buy-gun, Method: post }

  BuyProtectionFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ../backend/dist/
      Handler: backend/functions/player/buyProtection.handler
      Policies:
        - DynamoDBCrudPolicy: { TableName: !Ref PlayersTable }
      Events:
        BuyProtection:
          Type: Api
          Properties: { RestApiId: !Ref MafiosoApi, Path: /api/player/buy-protection, Method: post }

  SwissBankFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ../backend/dist/
      Handler: backend/functions/player/swissBank.handler
      Policies:
        - DynamoDBCrudPolicy: { TableName: !Ref PlayersTable }
      Events:
        SwissBank:
          Type: Api
          Properties: { RestApiId: !Ref MafiosoApi, Path: /api/player/swiss-bank, Method: post }

  TravelFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ../backend/dist/
      Handler: backend/functions/player/travel.handler
      Policies:
        - DynamoDBCrudPolicy: { TableName: !Ref PlayersTable }
      Events:
        Travel:
          Type: Api
          Properties: { RestApiId: !Ref MafiosoApi, Path: /api/player/travel, Method: post }

  TradeGoodsFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ../backend/dist/
      Handler: backend/functions/player/tradeGoods.handler
      Policies:
        - DynamoDBCrudPolicy: { TableName: !Ref PlayersTable }
        - DynamoDBReadPolicy: { TableName: !Ref CrewsTable }
        - Statement:
            - Effect: Allow
              Action: secretsmanager:GetSecretValue
              Resource: !Ref JWTSecretArn
      Events:
        TradeGoods:
          Type: Api
          Properties: { RestApiId: !Ref MafiosoApi, Path: /api/player/trade-goods, Method: post }

  ManageCrewFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ../backend/dist/
      Handler: backend/functions/crew/manageCrew.handler
      Policies:
        - DynamoDBCrudPolicy: { TableName: !Ref PlayersTable }
        - DynamoDBCrudPolicy: { TableName: !Ref CrewsTable }
        - Statement:
            - Effect: Allow
              Action: secretsmanager:GetSecretValue
              Resource: !Ref JWTSecretArn
      Events:
        CrewStatus:
          Type: Api
          Properties: { RestApiId: !Ref MafiosoApi, Path: /api/crew/status, Method: get }
        ManageCrew:
          Type: Api
          Properties: { RestApiId: !Ref MafiosoApi, Path: /api/crew/manage, Method: post }

  SearchPlayerFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ../backend/dist/
      Handler: backend/functions/combat/searchPlayer.handler
      Policies:
        - DynamoDBCrudPolicy: { TableName: !Ref PlayersTable }
      Events:
        Search:
          Type: Api
          Properties: { RestApiId: !Ref MafiosoApi, Path: /api/combat/search-player, Method: post }

  ShootPlayerFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ../backend/dist/
      Handler: backend/functions/combat/shootPlayer.handler
      Policies:
        - DynamoDBCrudPolicy: { TableName: !Ref PlayersTable }
      Events:
        Shoot:
          Type: Api
          Properties: { RestApiId: !Ref MafiosoApi, Path: /api/combat/shoot-player, Method: post }

  CancelSearchFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ../backend/dist/
      Handler: backend/functions/combat/cancelSearch.handler
      Policies:
        - DynamoDBCrudPolicy: { TableName: !Ref PlayersTable }
      Events:
        CancelSearch:
          Type: Api
          Properties: { RestApiId: !Ref MafiosoApi, Path: /api/combat/cancel-search, Method: post }

  GetCarsFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ../backend/dist/
      Handler: backend/functions/garage/getCars.handler
      Policies:
        - DynamoDBCrudPolicy: { TableName: !Ref PlayersTable }
      Events:
        Cars:
          Type: Api
          Properties: { RestApiId: !Ref MafiosoApi, Path: /api/garage/cars, Method: get }

  ActivateCarFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ../backend/dist/
      Handler: backend/functions/garage/activateCar.handler
      Policies:
        - DynamoDBCrudPolicy: { TableName: !Ref PlayersTable }
      Events:
        Activate:
          Type: Api
          Properties: { RestApiId: !Ref MafiosoApi, Path: /api/garage/activate, Method: post }

  RepairCarFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ../backend/dist/
      Handler: backend/functions/garage/repairCar.handler
      Policies:
        - DynamoDBCrudPolicy: { TableName: !Ref PlayersTable }
      Events:
        Repair:
          Type: Api
          Properties: { RestApiId: !Ref MafiosoApi, Path: /api/garage/repair, Method: post }

  MeltCarFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ../backend/dist/
      Handler: backend/functions/garage/meltCar.handler
      Policies:
        - DynamoDBCrudPolicy: { TableName: !Ref PlayersTable }
      Events:
        Melt:
          Type: Api
          Properties: { RestApiId: !Ref MafiosoApi, Path: /api/garage/melt, Method: post }

  SearchListingsFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ../backend/dist/
      Handler: backend/functions/marketplace/searchListings.handler
      Policies:
        - DynamoDBReadPolicy: { TableName: !Ref CarListingsTable }
      Events:
        Listings:
          Type: Api
          Properties: { RestApiId: !Ref MafiosoApi, Path: /api/marketplace/listings, Method: get }

  ListCarFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ../backend/dist/
      Handler: backend/functions/marketplace/listCar.handler
      Policies:
        - DynamoDBCrudPolicy: { TableName: !Ref PlayersTable }
        - DynamoDBCrudPolicy: { TableName: !Ref CarListingsTable }
      Events:
        ListCar:
          Type: Api
          Properties: { RestApiId: !Ref MafiosoApi, Path: /api/marketplace/list, Method: post }

  BuyCarFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ../backend/dist/
      Handler: backend/functions/marketplace/buyCar.handler
      Policies:
        - DynamoDBCrudPolicy: { TableName: !Ref PlayersTable }
        - DynamoDBCrudPolicy: { TableName: !Ref CarListingsTable }
      Events:
        BuyCar:
          Type: Api
          Properties: { RestApiId: !Ref MafiosoApi, Path: /api/marketplace/buy, Method: post }

  GetFactoryStatusFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ../backend/dist/
      Handler: backend/functions/bulletfactory/getFactoryStatus.handler
      Policies:
        - DynamoDBReadPolicy: { TableName: !Ref PlayersTable }
        - DynamoDBReadPolicy: { TableName: !Ref BulletFactoriesTable }
      Events:
        FactoryStatus:
          Type: Api
          Properties: { RestApiId: !Ref MafiosoApi, Path: /api/bulletfactory/status, Method: get }

  CollectBulletsFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ../backend/dist/
      Handler: backend/functions/bulletfactory/collectBullets.handler
      Policies:
        - DynamoDBCrudPolicy: { TableName: !Ref PlayersTable }
        - DynamoDBCrudPolicy: { TableName: !Ref BulletFactoriesTable }
      Events:
        CollectBullets:
          Type: Api
          Properties: { RestApiId: !Ref MafiosoApi, Path: /api/bulletfactory/collect, Method: post }

  TakeoverFactoryFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ../backend/dist/
      Handler: backend/functions/bulletfactory/takeoverFactory.handler
      Policies:
        - DynamoDBCrudPolicy: { TableName: !Ref PlayersTable }
        - DynamoDBCrudPolicy: { TableName: !Ref BulletFactoriesTable }
      Events:
        TakeoverFactory:
          Type: Api
          Properties: { RestApiId: !Ref MafiosoApi, Path: /api/bulletfactory/takeover, Method: post }

  GetCityPropsFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ../backend/dist/
      Handler: backend/functions/props/getCityProps.handler
      Policies:
        - DynamoDBReadPolicy: { TableName: !Ref PlayersTable }
        - DynamoDBCrudPolicy: { TableName: !Ref CityPropsTable }
      Events:
        Props:
          Type: Api
          Properties: { RestApiId: !Ref MafiosoApi, Path: /api/props, Method: get }

  ClaimPropFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ../backend/dist/
      Handler: backend/functions/props/claimProp.handler
      Policies:
        - DynamoDBCrudPolicy: { TableName: !Ref PlayersTable }
        - DynamoDBCrudPolicy: { TableName: !Ref CityPropsTable }
      Events:
        ClaimProp:
          Type: Api
          Properties: { RestApiId: !Ref MafiosoApi, Path: /api/props/claim, Method: post }

  WebBucket:
    Type: AWS::S3::Bucket
    DeletionPolicy: RetainExceptOnCreate
    UpdateReplacePolicy: Retain
    Properties:
      BucketName: !Sub 'mafioso-${Stage}-${AWS::AccountId}-${AWS::Region}-web-v2'
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        IgnorePublicAcls: true
        BlockPublicPolicy: true
        RestrictPublicBuckets: true

  WebOriginAccessControl:
    Type: AWS::CloudFront::OriginAccessControl
    Properties:
      OriginAccessControlConfig:
        Name: !Sub 'mafioso-${Stage}-oac'
        OriginAccessControlOriginType: s3
        SigningBehavior: always
        SigningProtocol: sigv4

  WebDistribution:
    Type: AWS::CloudFront::Distribution
    Properties:
      DistributionConfig:
        Enabled: true
        DefaultRootObject: index.html
        PriceClass: PriceClass_100
        Origins:
          - Id: WebS3
            DomainName: !GetAtt WebBucket.RegionalDomainName
            OriginAccessControlId: !GetAtt WebOriginAccessControl.Id
            S3OriginConfig: {}
          - Id: GameApi
            DomainName: !Sub '${MafiosoApi}.execute-api.${AWS::Region}.amazonaws.com'
            OriginPath: !Sub '/${Stage}'
            CustomOriginConfig:
              OriginProtocolPolicy: https-only
        DefaultCacheBehavior:
          TargetOriginId: WebS3
          ViewerProtocolPolicy: redirect-to-https
          AllowedMethods: [GET, HEAD, OPTIONS]
          CachedMethods: [GET, HEAD, OPTIONS]
          Compress: true
          ForwardedValues:
            QueryString: false
            Cookies: { Forward: none }
        CacheBehaviors:
          - PathPattern: '/api/*'
            TargetOriginId: GameApi
            ViewerProtocolPolicy: https-only
            AllowedMethods: [GET, HEAD, OPTIONS, PUT, PATCH, POST, DELETE]
            CachedMethods: [GET, HEAD, OPTIONS]
            ForwardedValues:
              QueryString: true
              Headers: [Authorization, Origin, Content-Type]
              Cookies: { Forward: none }
            MinTTL: 0
            DefaultTTL: 0
            MaxTTL: 0
        CustomErrorResponses:
          - ErrorCode: 403
            ResponseCode: 200
            ResponsePagePath: /index.html
            ErrorCachingMinTTL: 0
          - ErrorCode: 404
            ResponseCode: 200
            ResponsePagePath: /index.html
            ErrorCachingMinTTL: 0

  WebBucketPolicy:
    Type: AWS::S3::BucketPolicy
    Properties:
      Bucket: !Ref WebBucket
      PolicyDocument:
        Statement:
          - Effect: Allow
            Principal: { Service: cloudfront.amazonaws.com }
            Action: s3:GetObject
            Resource: !Sub '${WebBucket.Arn}/*'
            Condition:
              StringEquals:
                AWS:SourceArn: !Sub 'arn:aws:cloudfront::${AWS::AccountId}:distribution/${WebDistribution}'

Outputs:
  ApiUrl:
    Value: !Sub 'https://${MafiosoApi}.execute-api.${AWS::Region}.amazonaws.com/${Stage}'
  UserPoolId:
    Value: !Ref UserPool
  UserPoolClientId:
    Value: !Ref UserPoolClient
  WebBucketName:
    Value: !Ref WebBucket
  CloudFrontDistributionId:
    Value: !Ref WebDistribution
  WebUrl:
    Value: !Sub 'https://${WebDistribution.DomainName}'
